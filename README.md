# Maze_solver
Maze Solver is a Python-based application designed to solve mazes using different pathfinding algorithms such as Breadth-First Search (BFS), Depth-First Search (DFS), and A* Search Algorithm.  The project includes:  A graphical user interface (GUI) version built with Tkinter for interactive visualization, and A console-based version .
